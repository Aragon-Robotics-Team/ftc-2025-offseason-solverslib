package org.firstinspires.ftc.teamcode;

public class Robot { //This class is a singleton- there is only one instance at a time
    private static Robot instance = new Robot();
    public static Robot getInstance() {
        if (instance == null) {
            instance = new Robot();
        }
        instance.
    }
}
